# config.py
import numpy as np

# Link Lengths (meters)
LINK_LENGTHS = np.array([0.6, 0.3, 0.1])

# Initial Joint Angles (radians)
INITIAL_JOINT_ANGLES = np.array([0, 0, 0])

# Starting Position
STARTING_POSITION = np.array([0, 0])
